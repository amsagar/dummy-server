{
  "name": "Decision Expression",
  "description": "Decision node using expression mode.",
  "graph": {
    "nodes": [
      { "id": "start", "type": "start", "label": "Start", "config": {} },
      { "id": "decision_1", "type": "decision", "label": "Check Amount", "config": { "expression": "$.chainInput.amount > 100 && $.chainInput.country == \"US\"", "trueBranch": "priority", "falseBranch": "normal" } },
      { "id": "priority", "type": "tool", "label": "Priority Path", "config": { "toolName": "priority_handler" } },
      { "id": "normal", "type": "tool", "label": "Normal Path", "config": { "toolName": "normal_handler" } },
      { "id": "end", "type": "end", "label": "End", "config": {} }
    ],
    "edges": [
      { "from": "start", "to": "decision_1" },
      { "from": "decision_1", "to": "priority" },
      { "from": "decision_1", "to": "normal" },
      { "from": "priority", "to": "end" },
      { "from": "normal", "to": "end" }
    ]
  }
}
