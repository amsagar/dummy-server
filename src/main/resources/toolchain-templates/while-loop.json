{
  "name": "While Iterator",
  "description": "Iterator with loopMode while and collectOutput.",
  "graph": {
    "variables": [
      { "name": "done", "type": "boolean", "default": false, "description": "Loop stop flag" }
    ],
    "nodes": [
      { "id": "start", "type": "start", "label": "Start", "config": {} },
      { "id": "iter_1", "type": "iterator", "label": "While Loop", "config": { "loopMode": "while", "exitCondition": "$.vars.done == true", "maxIterations": 100, "collectOutput": true, "toolName": "echo", "argMappings": { "items": "$.chainInput.items", "value": "$item" } } },
      { "id": "end", "type": "end", "label": "End", "config": {} }
    ],
    "edges": [
      { "from": "start", "to": "iter_1" },
      { "from": "iter_1", "to": "end" }
    ]
  }
}
